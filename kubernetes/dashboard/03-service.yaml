# service‐app.yaml
apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: dashboard
spec:
  selector:
    app: dashboard-frontend
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 3000
  type: ClusterIP     
